export type Usuario = {
    nom_usuario: string;
    contrasena: string;
    nombre: string;
    telefono: string;
    direccion: string;
    foto: string;
};

export type Credenciales = {
    nom_usuario: string,
    contrasena: string
};
